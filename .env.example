# Porta do servidor
PORT=3000

# Horário do envio diário em cron (padrão: '0 9 * * *' = todo dia às 09:00)
DAILY_CRON=0 9 * * *

# SMTP (ex.: Gmail/SendGrid/Outro)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass

# From do e-mail
EMAIL_FROM="Seu App <no-reply@seuapp.com>"
